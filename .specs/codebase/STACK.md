# Stack

- Python project with scripts under `py/` and a small root-level runner.
- pandas and NumPy are the primary data and numerical tools.
- SciPy, cvxpy, yfinance, matplotlib, seaborn, tqdm, requests, and related packages are listed in `py/requirements.txt`.
- There is no package build configuration beyond the untracked minimal `pyproject.toml`, and no supported test command is declared.
- The checked-in virtual environment currently lacks the listed scientific dependencies, so verification must report environment limitations explicitly.

